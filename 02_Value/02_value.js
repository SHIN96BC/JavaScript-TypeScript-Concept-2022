// Boolean
const flag = true;

// Null
const a = null;

// Undefiend
const b = undefined;

// Number
const c = 10;

// String 
const d = '가나다';

// 자바스크립트의 객체
const Color1 = {
    Red: 1,
    Blue: 2,
    Green: 3,
};

Color1.Red;

// Object 는 순수하게 객체이기 때문에 객체안에 어떤 값이든
// 내가 원하는대로 넣을 수 있다.